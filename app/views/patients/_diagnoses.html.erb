<div class="col-12 col-md-7 col-lg-9 text-center text-md-start mb-2 mb-md-0">
  <h4 class="fw-bold mb-1"><i class="fa-solid fa-stethoscope me-2 fs-5"></i> Diagnoses</h4>
  <p class="text-muted small mb-0">Track and manage patient diagnoses and treatment progress</p>
</div>
<div class="tab-pane fade show active" id="diagnoses">
  <% if @patient.diagnoses.present? %>
    <% @patient.diagnoses.order(created_at: :desc).each do |d| %>
      <div class="card mb-2 p-3">
        <h6><%= d.primary_diagnosis.humanize %></h6>
        <small class="text-muted"> ICD-10 <%= d.icd_code %></small>

        <div class="mt-2">
          <span class="badge bg-info"><%= d.severity.humanize %></span>
          <span class="badge bg-success"><%= d.status.humanize %></span>
        </div>

        <p class="mt-2"><strong>Symptoms:</strong> <%= d.symptoms.humanize %></p>
        <p><strong>Notes:</strong> <%= d.notes.humanize %></p>

        <small>Follow-up: <%= d.follow_up_date %></small>
      </div>
    <% end %>
  <% else %>
    <div class="alert alert-info">No Diagnoses found.</div>
  <% end %>
</div>
